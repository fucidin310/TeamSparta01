#include <iostream>
using namespace std;

int CalculateSum(int arr[], int size) {
	int sum = 0;
	for (int i = 0; i < size; i++) {
		sum += arr[i];
	}
	return sum;
}

float CalculateAverage(int arr[], int size) {
	return CalculateSum(arr, size) / (float)size;
}

int main() {	
	int Num[5];

	cout << "숫자를 입력하세요 : ";
	for (int i = 0; i < 5; i++) {
		cin >> Num[i];
	}

	cout << "합 : " << CalculateSum(Num, 5) << endl;
	cout << "평균 : " << CalculateAverage(Num, 5) << endl;

	return 0;
}
